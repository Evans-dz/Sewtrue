# Sew True

Zero-build static site with three Vercel functions (`api/`) for Stripe
checkout. Dev server: `.claude/launch.json` → port 4178.

Conventions, verify harnesses and the design contract live in the lab at
`/Users/dylanevans/Projects/EZHD/ezhd-lab`; the `ezhd-build` skill loads them.
Read `DESIGN.md` in this folder before changing markup or CSS.

Verify with `/ezhd-verify` before deploying. Never copy the harnesses in here.
This is a client site: ask before deploying.
